import {Character} from "../../character";
import {Decimal} from "decimal.js";

export const getGlobalState = jest.fn();

export const getCharacter = jest.fn();

export const reincarnateAs = jest.fn();

export const saveGlobalState = jest.fn();

export const generateCreature = jest.fn();